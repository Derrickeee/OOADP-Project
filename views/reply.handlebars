<div class="c">
<div>

		<h3>Reply</h3>
		<form action="/saveEditedFeedback/{{video.id}}?_method=PUT" method="post">
			<div class="row"> {{!—- row 1--}}
				<div class="col-lg-9">
					<div class="form-group">
                    	<p>Rating: 	{{{generateHTMLforLoop video.rating}}}</p>
                    <p>Comments: {{comments}}</p>
					</div>
					<div class="form-group">
						{{!—- Story --}}
						<label for="reply">Type your reply here</label>
						<textarea class="form-control" id="reply" name="reply" rows="12">{{video.reply}}</textarea>
					</div>

				<div class="form-group">
			<button id="butAddVideo" type="submit" class="btn btn-warning">Submit
			</button>
			</div>
</div>
</div>
</div>
</div>
		</form>